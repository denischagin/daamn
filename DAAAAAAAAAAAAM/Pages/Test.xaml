<Page x:Class="DAAAAAAAAAAAAM.Pages.Test"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DAAAAAAAAAAAAM.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Test"
     Loaded="Page_Loaded"
     
      >

    <Grid>
        <StackPanel>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
                <Button Content="Выход" HorizontalAlignment="Left" VerticalAlignment="Top" Click="Button_Click"/>
                <Label Content="Фильтр" />
                <TextBox x:Name="filter" Width="300" TextChanged="TextBox_TextChanged" />
            </StackPanel>

            <DataGrid x:Name="table"  Height="400"/>

            <Button Content="Изменить" Click="edit_Click" />
        </StackPanel>
    </Grid>
</Page>
